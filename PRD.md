# OneFlow — 一句话驱动的自动化工作流 Agent

产品文档（PRD）
版本：v0.1
状态：草稿待评审

---

## 1. 背景与目标

### 1.1 背景
当前市面上"语音/聊天助手"多为单轮问答，用户问一句答一句，无法真正"把事办成"。
OneFlow 定位为**任务导向的 Agent**：用户用一句自然语言下指令，Agent 自行拆解步骤、
多次调用工具、核对结果并汇报，最终把任务真正执行完毕。

ASR / TTS 只是最外层的可选语音输入输出壳，不是核心。核心是：
**LLM 决策 → function calling 执行 → 拿结果再决策** 的 agent loop。

### 1.2 目标
- 用一句话自然语言表达一个多步骤任务，Agent 自主规划并执行完成。
- 内置一组可插拔的工具函数，覆盖"查天气 / 记账 / 订酒店占位 / 查日程 / 计算"等场景。
- 全程云端：ASR / TTS / LLM 均为云服务 API，本地不跑模型。
- 语音壳可插拔：可接云语音，也可纯网页 / API / 命令行使用。

### 1.3 非目标
- 不做本地模型推理。
- 不做跨企业复杂 ERP 集成（单机工具演示为主）。
- 不追求完全金融级安全，但实现基本的安全与权限边界。

---

## 2. 目标用户与场景

### 2.1 用户
- 想体验"Agent 真的帮你干活"的个人用户。
- 想拿一个"能自主规划 + 调工具"的 Agent 做演示 / 毕设 / 模板售卖的开发者。

### 2.2 场景
- SaaS: "帮我查明天北京天气，顺便提醒我带伞"
- 记账: "晚饭花了 58，记一下" / "这个月餐饮花了多少"
- 日程: "明天下午 3 点安排和运营的会"
- 多步: "帮我订周五去上海的酒店，预算 500 内，先查一下有哪些选择"

---

## 3. 核心概念

- **用户指令 (User Command)**：用户一次自然语言输入。可以是文字，也可以是语音转写后的文字。
- **Agent Loop**：核心循环。LLM 每轮：理解当前状态 → 决定调用哪个工具（或直接回答）→
  执行工具 → 把结果喂回 LLM → 继续，直到任务完成或达到上限。
- **工具 (Tool / Function)**：Agent 可调用的外部能力，是 function calling 的最小单元。
  每个工具 = 名称 + 描述 + 输入参数 schema + 执行函数。
- **会话 (Conversation)**：一轮有上下文的 Agent 对话，可连续追问。

---

## 4. 功能需求（FR）

### FR-1 自然语言指令输入
- 支持文字输入（API / 网页 / CLI）。
- 支持可选语音输入：云 ASR 把语音转文字后进入同一入口。

### FR-2 Agent 自主规划（核心）
- 收到指令后，LLM 把任务拆成子步骤，不依赖预设剧本（自由规划）。
- 支持一次任务内连续调用多个工具（多步）。
- 每步执行结果回传给 LLM，LLM 据此决定下一步（带反馈的循环）。

### FR-3 工具调用（function calling，核心）
- 以结构化 JSON 方式声明工具（name / description / parameters）。
- LLM 输出工具调用意图 → 系统执行 → 结果回传。
- MVP 内置工具集（见 §6）。

### FR-4 结果汇报
- 任务完成后，Agent 用自然语言总结执行结果（做了什么、结果如何、下一步建议）。

### FR-5 多轮上下文
- 同一会话内保留上下文，支持追问与钻取（"再查一下下周五的"）。

### FR-6 失败处理与重试
- 工具调用失败时，错误信息回传 LLM，允许其换参数或换策略重试。
- 达到最大步数（如 15 步）强制终止并汇报，防止死循环。

### FR-7 可选语音输出
- 云 TTS 把 Agent 的最终/中间回复转成语音。
- 进阶级：支持流式 + 打断（barge-in）（P1 之后）。

### FR-8 会话管理
- 创建 / 读取 / 删除会话，按用户隔离。
- 记录工具调用轨迹，便于审计与回放（对毕设/演示加分）。

---

## 5. 非功能需求（NFR）

- **全云端**：LLM / ASR / TTS 全部云 API，无本地模型。
- **可扩展**：新增工具 = 新增一个工具函数 + schema 声明，热插拔，不改框架代码。
- **安全**：API Key 走环境变量 / 服务端配置，不外泄；工具函数做输入校验与沙箱。
- **易部署**：单体服务 + 配置文件即可跑通，支持 Docker 一键部署。
- **性能**：普通任务（≤5 步）端到端在数秒内完成；交互式语音目标首响 <1s。
- **可观测**：每次 agent loop 的思考 / 工具调用 / 结果可打印查看（调试友好）。

---

## 6. MVP 内置工具集

| 工具 | 说明 | 参数示例 |
|---|---|---|
| `get_weather` | 查天气（云端天气 API） | city, date |
| `add_expense` | 记账 | amount, category, note |
| `query_expense` | 查账（按月/分类汇总） | month, category |
| `schedule_event` | 建日程占位 | title, datetime |
| `list_schedule` | 查日程 | date |
| `calculate` | 安全表达式计算 | expression |
| `search_hotel` | 查酒店（真实酒店 API，用户自填 Key；未配报错） | city, date, budget |

说明：MVP 优先保证"天气(Open-Meteo 免费无 Key) + 记账 + 日程 + 计算"能真实跑通；
订酒店接真实 API，**除测试外禁止 mock**——未配置 Key 时明确报错，绝不伪造结果。

---

## 7. 总体架构

```
用户 ──(文字 / 云ASR)──> 入口(API / Web / CLI)
                          │
                          v
              ┌─────────────────────────┐
              │      Agent Engine       │
              │  LLM(带function calling) │◄── 云端 LLM API
              │        · 理解意图        │
              │        · 拆解步骤        │
              │        · 决定调工具      │
              └───────────┬─────────────┘
                          │ 返回工具调用(JSON)
                          v
                 Tool Registry (工具注册表)
                 · 校验入参 → 执行 → 返回结果
                 │
                 v
              ┌──────────────┐    ┌──────────────┐
              │ 云 ASR (可选) │    │ 云 TTS (可选)  │
              └──────────────┘    └──────────────┘
```

技术选型（全部云端；原则：能免 Key 就免，需 Key 的用户自填）：
- **LLM**（必填，用户自选厂商）：任一带 function calling 的云模型（OpenAI / Claude / 云端 Qwen 均可），
  用户自行填写提供商 + 模型 + Key。
- **后端**：Python（FastAPI），负责 agent loop 编排 + 工具注册 + 会话。
- **语音（P1）**：优先免费无 Key 的浏览器 Web Speech（ASR）/ edge-tts（TTS）；
  亦支持用户自填 Key 的云 ASR/TTS。
- **状态**：会话与记账/日程数据用**真数据库 SQLite**（SQLAlchemy）。

---

## 8. MVP 定义（先跑通什么）

MVP = 能对一句指令完成"多步工具调用"的最小闭环：
1. 文字输入一句指令。
2. Agent 自由拆解步骤，连续调用 ≥2 个工具完成任务。
3. 任务完成后自然语言汇报结果。
4. 支持同会话追问。
5. 全程打印 agent loop 轨迹（可观测）。

MVP 不含：语音输入输出、真实订酒店、流式打断、多用户并发。（P1 再加）

---

## 9. 分阶段计划

### P0 — MVP（本阶段）
- 选一个云端 LLM，配好 function calling。
- 实现 agent loop + 工具注册表 + 5 个核心工具（天气/记账/查账/日程/计算）。
- CLI 可跑通一句多步指令 + 轨迹打印。

### P1 — 语音 + 会话
- 接云 ASR（语音转指令）与云 TTS（语音答）。
- 会话管理 + 按用户隔离 + 追问。

### P2 — 进阶
- 流式 + 打断（barge-in）。
- 真实订酒店 / 更多行业工具。
- Web 前端界面 + Docker 部署。

---

## 10. 验收标准

### P0 验收
- [ ] 输入"帮我查明天北京天气，如果下雨就提醒我记一条带伞的账"，能完成
      `get_weather` → 判断 → `add_expense` 等多步调用并汇报。
- [ ] 一句指令能连续调用 ≥2 个工具。
- [ ] 工具失败时能自动改参数重试（或明确报错不卡死）。
- [ ] 达到最大步数能终止并汇报，不无限循环。
- [ ] 全程可打印 agent loop 轨迹。

### P1 验收
- [ ] 语音说一句话能进入 agent 流程并语音回答。
- [ ] 多用户会话隔离，可连续追问。

---

## 11. 风险与对策

| 风险 | 对策 |
|---|---|
| LLM 规划跑偏 / 幻觉参数 | 工具描述写清楚 + 入参强校验 + 出错回喂重试 |
| 工具无限循环 | 最大步数熔断 |
| API Key 泄露 | 环境变量 + 服务端配置，前端不接触 |
| 真实订票等有副作用 | 接入真实 API 前加确认步骤；MVP 不含真实下单副作用 |

---

## 12. 开放问题（待评审）
- LLM 哪个厂商做默认（默认可配，用户自选；可先给 OpenAI 与云端 Qwen 两个推荐模板）。
- 记账与日程数据持久化 → 已定：真数据库 SQLite。
- 订酒店真实 API 接哪家 → 已定：不 mock，用户自填 Key；具体免费源待定。
