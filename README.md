# OneFlow — 一句话驱动的贾维斯式 AI Agent

任务导向私人 AI 管家：用户用一句自然语言下指令，Agent 自行拆解步骤、多次调用工具（function calling）、
核对结果并汇报，把任务真正执行完毕。核心是「LLM 决策 → 调工具 → 拿结果再决策」的 agent loop。

除了被动应答，OneFlow 还会**主动服务**：定时任务到点自动执行并语音播报、日程临期提醒、
**情景关怀（明天下雨×你有日程→前一晚提醒带伞）**、每晚习惯洞察（发现你常做的事并建议沉淀为定时任务）。
所有工具用真实 API / 真实数据库，**除测试外禁止 mock**。

而且它能**自己长出新能力**：说一句「你要是能帮我查快递就好了」，贾维斯会复述确认需求（语音同音字自动纠偏）、
自己写工具代码和测试、在沙箱里验证，经你一键确认后合并热上线——分支隔离可回退，安全门禁贯穿全程。

它还是个有温度、有边界的管家：**情绪感知**（察觉你烦躁/疲惫时收紧话锋）、**数字分身**（说声「我不在」，外出期间按你留的规则代劳）、
**技能市场**（他会的装给你，你学的可导出分享）、**年度体检**（每周重放历史调用自我打分）、
**记忆认知**（矛盾自动消解、久未用的自然遗忘）、**数据主权**（全部个人数据一键导出/彻底清除）。

> 加新功能前先读 [ARCHITECTURE.md](ARCHITECTURE.md) 的落位规则——把代码放对地方，别再堆成大文件。

## 技术栈
Python 3.12 · FastAPI · SQLite + SQLAlchemy · LiteLLM（多厂商 LLM）· APScheduler（后台调度）·
ddgs + BeautifulSoup（联网）· 云端 Embedding（语义记忆，零本地负担）·
React 18 + TypeScript + Vite · react-three-fiber（3D 全息核心）· PWA ·
**Electron（桌面壳）· sherpa-onnx（本地流式语音识别，离线）** · pytest

## 项目结构
```
app/                        后端
├── agent/                  agent loop：engine 主循环 + memory_recall / confirm / subagent / llm / context
├── jobs/                   后台主动服务注册表：提醒·关怀·洞察·数字分身·面试·体检…（一个职责一个模块）
├── learn/                  自学习：技能工厂·习惯结晶·情绪感知·反向面试·年度体检
├── routers/                API 路由（一个域一个文件）  ├── tools/  agent 工具（registry 统一注册）
├── scheduler.py            30s 轮询 tick：到期任务 + jobs 注册表遍历
└── models.py / db.py / main.py
frontend/src/               前端
├── api/                    每个后端路由一个域文件（共用 http.ts 实例）
├── hooks/                  聊天页各能力 hook（待命/播报/发送/通知…自包含）
├── lib/speech/             语音栈：wake 唤醒状态机 / asr+local 双识别通道 / playback 播报管道
├── pages/settings/         设置页区块组件（每区块自包含，页面只做布局）
└── components/ pages/ theme/ store/
```
> 各目录的落位规则与防拥挤红线详见 [ARCHITECTURE.md](ARCHITECTURE.md)——加新功能前先读它。

## 快速开始

```bash
cd oneflow-agent
source .venv/bin/activate        # 若未创建：python3 -m venv .venv
# 1. 配置 .env（把你的 LLM 密钥填进去；也可在网页「设置」里每用户自填）
#    LLM_PROVIDER / LLM_MODEL / LLM_API_KEY
# 2. 启动后端（端口 8020，--reload 自动热更）
uvicorn app.main:app --port 8020 --reload
# 3. 启动前端（另开终端）
cd frontend && npm install && npm run dev   # → http://localhost:5178（/api 代理到 8020）
# 4. 验证
curl http://127.0.0.1:8020/api/health
```

打开 http://localhost:5178 注册登录，进「设置」填 LLM 密钥即可对话。页面可**安装为 PWA**（Dock 常驻）。

## 桌面端（推荐，语音体验完全体）

浏览器版语音识别依赖 Chrome→Google 服务（国内不稳定）；桌面端用 **sherpa-onnx 本地流式识别**，
唤醒/识别全程离线、毫秒级响应，且音频播放无浏览器自动播放限制。

```bash
# 1. 构建前端（FastAPI 会托管 dist，Electron 直接加载 :8020）
cd frontend && npm run build

# 2. 安装桌面端依赖 + 下载本地语音模型（约 100MB，仅首次）
cd ../desktop && npm install && npm run models

# 3. 启动（自动拉起后端、托盘常驻、关闭窗口即隐身到托盘）
npm start

# 4. 打包独立 .app（输出 release/mac-arm64/OneFlow.app，本地 ad-hoc 签名）
npm run dist
```

> 未下载模型时桌面端自动回退浏览器 Web Speech，页面会提示下载命令。
> 本地识别用流式 zipformer 中英双语模型（内存占用约 100~200MB，M1 无压力），断句由引擎内置端点检测完成。
> 自打包 .app 为 ad-hoc 签名，首次打开若被 macOS 拦截：右键 →「打开」，或在「系统设置 → 隐私与安全性」点「仍要打开」（仅需一次）。

## 贾维斯体验

| 能力 | 说明 |
|---|---|
| 语音唤醒 | 常驻待命，唤醒词「贾维斯 / 小翼 / 你好小助手」，**同音字也能唤醒**；桌面端本地识别离线可用，浏览器端走 Web Speech。唤醒成功有上行音效 + **按时段问候** |
| 会话模式 | 唤醒即进入长对话：免唤醒词直接连说，直到说「退下吧」（含同音/别名容错）或闲置 90 秒贾维斯自己告退；遥测条显示“对话中” |
| 俏皮话 | 唤醒后没下文，3.5 秒后贾维斯主动搭话：按空唤醒次数递进情绪（礼仪→疑惑→小情绪→戏剧化），感知深夜/刚播报情境，洗牌不重复 + 5% 彩蛋 |
| 分句流式播报 | 回复按句切分，首句合成完即开播（出声延迟从“全文”降到“首句”）；**预取流水线**：后续句子合成与播放重叠，句间无空白；单句合成失败自动回退浏览器语音；文字与声音对齐（播完才淡出） |
| 免唤醒连续对话 | 会话模式**跨播报保持**：回复播报期间暂停监听但保留会话，播完直接说下一句即当指令；闲置 90 秒或说「退下」才结束（播报暂停不再误杀会话） |
| 播报防回声 | 播报期间自动暂停监听、真实播完才恢复，彻底避免把 AI 自己的声音识别成指令 |
| 忙时反馈 | 上一件事还在处理时再说指令，会明确提示“正在处理上一件事”，不再静默丢弃 |
| 贾维斯人格 | 英式管家口吻：称呼"先生"，"遵命/已为您办妥"，先结论后细节 |
| 情绪感知 | 消息进 agent 前轻量 LLM 分类情绪（calm/frustrated/tired，结果缓存 10 分钟）；烦躁/疲惫时自动收紧回复姿态，不再长篇大论 |
| 高危操作确认 | 记账/建定时任务等写操作**先问再执行**，语音说"确认"或点确认条均可 |
| 自学习·技能工厂 | 说「你要是能X就好了」→ 复述确认（ASR 同音字 LLM 自动纠偏）→ 自己写工具+测试 → Docker/子进程双沙箱验证 → 提案卡片一键上线热生效；缺 Key 自动提醒补填；分支隔离、revert 可回退 |
| 习惯结晶 | 每晚聚类近 7 天重复行为，同一操作 ≥3 次 → LLM 起草技能提案并主动询问；拒绝过的模式永久免打扰 |
| 技能复用检索 | 学习请求先查已学技能的语义索引：会了的直接用不重建，「升级 xxx」才重学 |
| 信任旋钮 | 三档自治等级用户可调：每步问我 / 标准（高危确认）/ 全自动；学习提案审批任何档位都不跳过 |
| 用量透明 | 全部 LLM 调用按场景埋点（对话/学习构建/夜间任务），/api/usage/stats 随时可查本月消耗 |
| 技能反馈环 | 上线技能运行质量监控：成功率 <50% 自动降级并说明，≥90% 好评反馈一次；重新学习即可恢复 |
| 技能市场 | 浏览/安装/导出所有用户已上线的技能；安装走同一套安全门禁 + 沙箱重验，「他会的」一键变成「你也会」 |
| 年度体检 | 抽样最近成功调用为「金题集」，每周静默重放打分（直接工具调用，零 LLM 开销）；成功率 <80% 自动投递 ⚠️ 健康报告 |
| 数字分身 | 聊天说「我不在」开启（引导把外出规则存入 away_rules）、「我回来了」关闭；调度器每 tick 检查规则，LLM 判定此刻是否需要动作（如日程变更），执行并留「数字分身代劳」通知，每规则每天最多一次 |
| MCP 生态接入 | 设置页添加任意 MCP server → 其工具自动注册为 `mcp_<server>_<tool>`，agent loop 无差别调用；信任旋钮按服务设档、用量照常埋点；命令白名单防任意执行 |
| 反向面试 | 每周一次主动提问补画像盲区（缺城市问城市、无记忆问偏好），LLM 生成有好奇心的提问；聊天回「面试 你的回答」自动入库并致谢 |
| 情境剧本 | 「场景 出差」一句触发多步任务顺序编排，单步失败不断链；设置页可视化创建/启停/删除/立即执行 |
| 影子模式 | 会话内实时识别重复流程（30 分钟 ≥3 条指令）→ 主动提议「要不要存成剧本？」→ 回「存为剧本」一键落库即刻可用 |
| 成长档案 | /journal 页：学会技能数、累计调用量、里程碑时间线——贾维斯与你的共同历史，越用越厚 |
| 真流式 | SSE 实时推送：工具调用进度（`CALL xxx 调用中…`）+ 最终回复逐 token 打字机 |
| 语音播报 | 后端 edge-tts 合成 mp3（免费无 Key），**音色任选**（晓晓/云希/云健…），真实播完才恢复监听 |
| 主动播报 | 定时任务/日程提醒/**情景关怀**/习惯洞察 → 前端轮询拉取 → 页内展示 + 语音播报 + 系统通知（页面隐藏时） |
| 晨间简报 | 设置页一键开启：每天到点自动查天气+日程并主动播报（贾维斯式早安） |
| 情景关怀 | 每晚自动检查：明天有雨/雪 × 明天有日程 → 前一晚提醒"带伞并提前出发"（纯规则，零 LLM 开销） |
| 习惯学习 | 每晚统计近 7 天工具使用，高频行为主动建议沉淀为定时任务（每人每天最多一次） |
| 闲置轻推 | 长时间无交互时贾维斯偶尔主动说一句（待确认操作/临期日程/今日有雨/深夜问候，每会话最多 2 次，可关） |
| 失败自报 | 定时任务/后台规则异常时主动告知（错误人话化、只展示不播报、每天最多一条） |
| 用户画像 | Agent 自动维护城市/称呼等画像（set_profile），简报与播报个性化 |
| 语义记忆 | 云端 embedding 向量召回 top-k（不支持的厂商自动回退全量注入），零本地内存负担 |
| 记忆认知 | remember 前先语义检索旧记忆交 LLM 判定：**矛盾→旧记忆标 superseded**、重复→跳过、补充→合并文本；召回按闲置天数**遗忘衰减**（90 天未访问且低权重不再注入）；反向面试升级为**主题递进**（连续 4 周同主题，参考上周答案，满 4 周自动换题） |
| 数据主权 | 一键**导出**全部个人数据（记忆/画像/会话/提案/任务等单份 JSON）；**清除**需显式确认（confirm=DELETE），按外键层级自下而上真删并返回各表行数收据 |
| 自定义子智能体 | 用户自建"专家团队"（人设+工具白名单），delegate 动态路由 |
| 3D 全息核心 | react-three-fiber 全息 AI 核心：待机呼吸、思考高速自转、**播报时随语音律动发光** |

> 语音识别用浏览器 Web Speech（Chrome 需可达 Google 服务，失败时页面会明确提示并自动关闭待命）。
> 语音识别/播报需真实浏览器并允许麦克风；headless 环境不可用。

## LLM 自选厂商（.env 或网页设置）
| 厂商 | LLM_PROVIDER | LLM_MODEL 示例 | 说明 |
|---|---|---|---|
| OpenAI | openai | gpt-4o-mini | 填你的 OpenAI Key；embedding 用 text-embedding-3-small |
| 云端 Qwen（百炼） | openai | qwen/qwen-plus | 兼容模式，LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1；embedding 用 text-embedding-v3 |
| DeepSeek | deepseek | deepseek-chat / deepseek-reasoner | 思考模型已兼容（思维链多轮自动回传）；无语义记忆（自动降级） |
| Anthropic | anthropic | claude-… | 填你的 Key |

> LLM 密钥既可在 .env 全局配置，也可在网页「设置」里每用户自填（agent 优先用用户配置）。
> 未填密钥时接口返回明确提示；瞬时错误（网络/限流/5xx）自动重试；调用失败不落库、不污染历史；
> 长会话历史按 token 预算自动裁剪（CONTEXT_BUDGET_CHARS，默认 48000 字符）。

## 内置工具（19 个，真实数据，无 mock）
| 工具 | 数据源 | Key |
|---|---|---|
| get_weather | Open-Meteo | 免费无 Key |
| calculate | ast 标准库安全求值 | 本地 |
| add_expense / query_expense | SQLite 真数据库（写操作需确认） | 本地 |
| schedule_event / list_schedule | SQLite 真数据库 | 本地 |
| remember | 长期记忆：记住偏好/事实，云端向量化，语义召回 | SQLite 真库 |
| set_profile / get_profile | 用户画像（城市/称呼等） | SQLite 真库 |
| search_hotel | 用户自填 URL+Key | 需用户配置 |
| delegate | 多智能体编排：委派内置/自定义子智能体（均可联网） | 内置 |
| create/list/cancel_scheduled_task | 定时任务：到点自动跑 agent 并主动播报（写操作需确认） | 本地 |
| create/list/delete_custom_agent | 自定义子智能体管理 | 本地 |
| web_search | DuckDuckGo 联网搜索 | 免费无 Key |
| read_webpage | 网页正文提取（去噪+截断） | 免费 |

## 自学习 · 技能工厂（Agent 自己长出新能力）

对贾维斯说「你要是能帮我查快递就好了」，触发完整闭环：

1. **确认**：秒回复述需求等你确认——语音识别的同音字错误（快递→会计）由 LLM 结合语境自动纠偏，
   你看到理解偏差直接发正确需求即视为纠正；「取消」随时放弃
2. **构建**：LLM 生成工具模块 + pytest 测试 → 静态安全门禁（禁 eval/exec/subprocess/文件读写等危险面）→
   双模式沙箱验证（Docker 无网络容器优先，自动回退子进程），失败带报错自动重试（≤2 轮）
3. **审批上线**：候选提交在独立分支（git worktree 隔离，主工作区零扰动）；
   聊天回复「批准 N」或点提案卡片「合并上线」→ merge + 工具热注册，**无需重启立即生效**；
   缺 Key 会列出清单，聊天补填「key N 名称=值」即可（落库保存、重启自动回注）
4. **反悔**：「拒绝 N」删分支清现场零残留；已上线的技能 `git revert -m 1` 整体回退

安全边界：核心代码对 Agent 只读（只能新增 skills 目录）· 候选分支隔离 ·
构建使用用户自己的 LLM 配置 · 相同请求去重 · 同用户构建串行 · 外部命令进程组级超时强杀。

API：POST /api/learn/acquire（发起学习）· GET /api/learn/proposals（提案列表）·
POST /api/learn/proposals/{id}/approve · /reject（审批）

技能市场：GET /api/market/skills（浏览所有已上线技能）· GET /api/market/skills/{id}/export（导出）·
POST /api/market/skills/{id}/install（安装 = 既有门禁 + 沙箱重验后热上线）

> 配套机制：夜间**习惯结晶**自动起草提案 · 已学技能**语义检索复用**（不重复造轮子）·
> **运行反馈环**差评自动降级 · **用量埋点**全场景可查（GET /api/usage/stats）

## 后台调度（APScheduler）
- **30s 轮询**：到期定时任务在用户「定时播报」会话里自动跑一遍 agent（可自己调工具）→ 产出通知
- **日程提醒**：日程开始前 5 分钟自动推送提醒（防重复）
- **情景关怀**：每晚 20 点后，明天有雨/雪 × 明天有日程 → 前一晚主动提醒（城市取画像，无画像默认上海）
- **习惯学习**：每晚 21 点后统计近 7 天工具使用，高频行为（≥3 次）主动建议沉淀为定时任务
- **失败自报**：LLM 类错误转人话并标 system_error（前端只展示不播报）；规则块异常各自兜底，每天最多一条防刷屏
- **数字分身**：每 tick 检查启用的外出规则，LLM 判定此刻是否需要动作并代劳（每规则每天最多一次，避免反复折腾）
- **年度体检**：每周为每用户重放「金题集」（最近成功调用抽样）打分，成功率 <80% 投递 ⚠️ 健康通知（周节流）
- 一次性任务跑完自动停用；周期任务自动推进到下一次；服务重启即自愈

## 接口一览
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /api/auth/register · /login | 注册 / 登录（JWT） |
| GET | /api/auth/me | 当前用户 |
| POST | /api/chat | 发指令 → {reply, steps, trace} |
| POST | /api/chat/stream | SSE 真流式：step / delta / **confirm（高危确认）** / error / done |
| GET/POST/DELETE | /api/conversations… | 会话管理 / 消息 / 轨迹 |
| GET | /api/notifications?unread= | 主动通知（定时播报/日程提醒/情景关怀/习惯洞察/系统自检） |
| POST | /api/notifications/{id}/read | 标记已读 |
| DELETE | /api/notifications/{id} | 删除一条通知（通知中心用） |
| GET/PUT/DELETE | /api/tasks… | 定时任务管理（列表排除晨间简报；启停/改时间/删除） |
| GET | /api/idle-hint | 闲置轻推话题（待确认→临期日程→今日天气→深夜问候，无可说时返回 null） |
| GET/PUT | /api/briefing | 晨间简报开关与时间 |
| GET/PUT | /api/settings/llm · /hotel | 每用户 LLM / 订酒店配置 |
| POST | /api/tts | edge-tts 语音合成（voice 参数可选音色）→ audio/mpeg |
| GET/DELETE | /api/memories… | 长期记忆查看/删除 |
| POST | /api/learn/acquire | 自学习：发起「学会新能力」构建（确认式流程） |
| GET | /api/learn/proposals | 技能提案列表 |
| POST | /api/learn/proposals/{id}/approve · /reject | 提案审批（批准 = merge + 热注册；可携带补填 Key） |
| GET | /api/market/skills | 技能市场：浏览所有用户已上线技能 |
| GET/POST | /api/market/skills/{id}/export · /install | 技能导出 / 安装（安装走门禁 + 沙箱重验后热上线） |
| GET | /api/journal | 成长档案：技能数/调用量/里程碑时间线 |
| GET | /api/privacy/export | 数据主权：一键导出全部个人数据（单份 JSON） |
| DELETE | /api/privacy/purge | 数据主权：彻底清除全部数据（需 confirm=DELETE，返回各表行数收据） |
| GET | /api/health | 健康检查 |

## 测试
```bash
.venv/bin/python -m pytest tests/ -q   # 258 个用例全绿（仅测试允许 mock）
```
覆盖：agent loop / 真流式 SSE / 错误不落库 / 上下文裁剪 / 思维链回传 / 高危确认流程 /
自定义子智能体委派 / 画像注入 / 语义召回降级 / 调度引擎（任务执行、日程提醒、
情景关怀、习惯洞察、**失败自报**）/ 晨间简报 / **任务管理** / **闲置轻推** / 联网工具 / 各工具与鉴权 /
**自学习管线**（安全门禁 / 双模式沙箱 / 两段式确认 / 审批热注册 / 去重与并发锁）/
**信任体系**（信任旋钮 / 习惯结晶提案 / 检索复用 / 用量埋点 / 运行反馈环）/
**记忆认知**（矛盾替换 / 重复跳过 / 补充合并 / 遗忘衰减排序 / 反向面试主题递进）/
**数字分身与情绪感知** / **技能市场**（浏览/安装/导出）/ **年度体检**（金题重放打分）/ **数据主权**（导出/清除收据）。

## 验收状态
- ✅ P0 后端：登录鉴权 · agent loop 多步工具调用 · 真实工具 · 无 mock
- ✅ 多智能体编排：delegate 委派内置生活/财务/出行 + **用户自定义子智能体**，汇总回报
- ✅ 长期记忆：remember + 云端 embedding 语义召回（自动降级）+ **用户画像**
- ✅ 贾维斯语音：同音字容错唤醒 · 免唤醒连续对话 · barge-in 可打断 · **音色任选** · **时段问候** · 唤醒音效
- ✅ 贾维斯人格：英式管家口吻 system prompt
- ✅ 高危操作确认：写操作先问再执行（SSE confirm 事件 + 前端确认条 + 语音确认）
- ✅ 真流式 SSE：工具进度 + 逐 token 回复；错误处理；历史按预算裁剪
- ✅ 主动服务：定时任务播报 · 日程提醒 · 晨间简报一键开启 · 情景关怀 · 习惯洞察 · **闲置轻推**
- ✅ 可观测性：**调度/规则失败自报**（人话化、防刷屏、只展示不播报）
- ✅ 管理 UI：**定时任务管理**（启停/删除，晨间简报单一控制源）+ **通知中心**（历史回看/删除/清空）
- ✅ 联网能力：web_search + read_webpage，子 Agent 均可用
- ✅ DeepSeek 思考模型兼容（reasoning_content 多轮回传 + 落库）
- ✅ PWA：manifest + Service Worker，可安装到 Dock；页面隐藏时系统通知
- ✅ 语音交互升级：会话模式（唤醒即长对话/退下词退出/闲置告退/**跨播报保持**）· 递进式俏皮话 · 分句流水线播报（首句即播/**预取无空白**/失败回退/声画对齐）
- ✅ 桌面端：Electron 壳（自动拉起后端/托盘常驻/音频策略解锁）+ sherpa-onnx 本地流式识别（离线唤醒与转写，自动回退 Web Speech）+ **electron-builder 打包**（npm run dist）
- ✅ 自学习·技能工厂：两段式确认流（ASR 听写纠错）· 安全门禁 + 双模式沙箱 · 分支隔离可回退 · 缺 Key 补填 · 提案卡片 · 费用与并发防护
- ✅ 信任体系：**信任旋钮**三档自治 · **习惯结晶**自动提案 · 技能检索复用 · 用量埋点透明 · **运行反馈环**差评降级
- ✅ 创意功能线：**反向面试**（主动采访补画像）· **情境剧本**（一句编排多步）· **影子模式**（实时提议存剧本）· **成长档案**（/journal 时间线）· MCP 生态接入
- ✅ 记忆认知：冲突消解（矛盾替换 / 重复跳过 / 补充合并）· 遗忘衰减（闲置降权 + 90 天不再注入）· 反向面试主题递进
- ✅ 数字分身与情绪感知：外出规则 LLM 判定代劳（每日限次）· 情绪分类收紧回复姿态（10 分钟缓存）
- ✅ 技能市场：浏览 / 安装（门禁+沙箱重验）/ 导出已上线技能 · **年度体检**（金题集每周重放打分 + 健康通知）
- ✅ 数据主权：全部个人数据一键导出（单份 JSON）· 确认式彻底清除（外键层级真删 + 行数收据）
- ✅ 3D 全息前端：全息核心随播报律动 · 深空 HUD · 打字机/动效 · 功能全保留
- ✅ 结构治理：设置页/聊天页/语音栈/API 层/scheduler/engine 全部模块化拆分 · 258 测试全绿 · 落位规则见 ARCHITECTURE.md
- ⏭ 下一步可选：拍照/截图记账（多模态，需视觉模型）· 本地唤醒词模型（keyword spotter）· 真 Web Push · RAG · Telegram 入口 · 长程任务规划（动态拆解与重排）
